// write the code from node and paste it here

//Object.freeze(obj)->this makes the object immutable,ie you can't change the
// properties or add/remove new key:value pairs

//Object.seal(obj)->this allows you to update the values of the properties (keys), 
// but you can't add new/remove key:value pairs
